# Regenerated by git-hooks.nix on `nix develop` — a symlink into /nix/store.
.pre-commit-config.yaml

# `nix build` output symlinks.
/result
/result-*

# Python build / tool artifacts.
__pycache__/
*.egg-info/
.ruff_cache/
/build/
/dist/
